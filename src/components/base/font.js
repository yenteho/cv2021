import styled from 'styled-components'

export const F32 = styled.h1`
  font-size: 3.2rem;
  font-weight: ${(props) => props.bold && 'bold'};
  font-stretch: ${(props) =>
    props.condensed && props.theme.isVN
      ? 'normal'
      : props.condensed
      ? 'condensed'
      : 'normal'};
  text-transform: ${(props) => props.uppercase && 'uppercase'};
`
export const F24 = styled.h1`
  font-size: 2.4rem;
  font-weight: ${(props) => props.bold && 'bold'};
  font-stretch: ${(props) =>
    props.condensed && props.theme.isVN
      ? 'normal'
      : props.condensed
      ? 'condensed'
      : 'normal'};
  text-transform: ${(props) => props.uppercase && 'uppercase'};
`
export const F22 = styled.h1`
  font-size: 2.2rem;
  font-weight: ${(props) => props.bold && 'bold'};
  font-stretch: ${(props) =>
    props.condensed && props.theme.isVN
      ? 'normal'
      : props.condensed
      ? 'condensed'
      : 'normal'};
  text-transform: ${(props) => props.uppercase && 'uppercase'};
`
export const F20 = styled.h1`
  font-size: 2rem;
  line-height: 2.2rem;
  font-weight: ${(props) => props.bold && 'bold'};
  font-stretch: ${(props) =>
    props.condensed && props.theme.isVN
      ? 'normal'
      : props.condensed
      ? 'condensed'
      : 'normal'};
  text-transform: ${(props) => props.uppercase && 'uppercase'};
`
export const F18 = styled.h3`
  font-size: 1.8rem;
  line-height: 2rem;
  font-weight: ${(props) => props.bold && 'bold'};
  font-stretch: ${(props) =>
    props.condensed && props.theme.isVN
      ? 'normal'
      : props.condensed
      ? 'condensed'
      : 'normal'};
  text-transform: ${(props) => props.uppercase && 'uppercase'};
`
export const F16 = styled.h3`
  font-size: 1.6rem;
  line-height: 1.8rem;
  font-weight: ${(props) => props.bold && 'bold'};
  font-stretch: ${(props) =>
    props.condensed && props.theme.isVN
      ? 'normal'
      : props.condensed
      ? 'condensed'
      : 'normal'};
  text-transform: ${(props) => props.uppercase && 'uppercase'};
`
export const F15 = styled.h3`
  font-size: 1.5rem;
  line-height: 1.7rem;
  font-weight: ${(props) => props.bold && 'bold'};
  font-stretch: ${(props) =>
    props.condensed && props.theme.isVN
      ? 'normal'
      : props.condensed
      ? 'condensed'
      : 'normal'};
  text-transform: ${(props) => props.uppercase && 'uppercase'};
`
export const F14 = styled.h4`
  font-size: 1.4rem;
  line-height: 1.6rem;
  font-weight: ${(props) => props.bold && 'bold'};
  font-stretch: ${(props) =>
    props.condensed && props.theme.isVN
      ? 'normal'
      : props.condensed
      ? 'condensed'
      : 'normal'};
  text-transform: ${(props) => props.uppercase && 'uppercase'};
`
export const F13 = styled.h4`
  font-size: 1.3rem;
  line-height: 1.5rem;
  font-weight: ${(props) => props.bold && 'bold'};
  font-stretch: ${(props) =>
    props.condensed && props.theme.isVN
      ? 'normal'
      : props.condensed
      ? 'condensed'
      : 'normal'};
  text-transform: ${(props) => props.uppercase && 'uppercase'};
`
export const F12 = styled.h4`
  font-size: 1.2rem;
  line-height: 1.4rem;
  font-weight: ${(props) => props.bold && 'bold'};
  font-stretch: ${(props) =>
    props.condensed && props.theme.isVN
      ? 'normal'
      : props.condensed
      ? 'condensed'
      : 'normal'};
  text-transform: ${(props) => props.uppercase && 'uppercase'};
`
export const F11 = styled.h4`
  font-size: 11px;
  line-height: 13px;
  font-weight: ${(props) => props.bold && 'bold'};
  font-stretch: ${(props) =>
    props.condensed && props.theme.isVN
      ? 'normal'
      : props.condensed
      ? 'condensed'
      : 'normal'};
  text-transform: ${(props) => props.uppercase && 'uppercase'};
`
export const F10 = styled.h4`
  font-size: 10px;
  line-height: 12px;
  font-weight: ${(props) => props.bold && 'bold'};
  font-stretch: ${(props) =>
    props.condensed && props.theme.isVN
      ? 'normal'
      : props.condensed
      ? 'condensed'
      : 'normal'};
  text-transform: ${(props) => props.uppercase && 'uppercase'};
`
